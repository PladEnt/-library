<h1><%= @user.name %></h1>
<ul>
  <% @user.reviews.each do |review| %>
    <li><%= link_to review.title, artist_song_path(@user, review) %></li>
  <% end %>
  <form method="get" action="/books">
    <input type="submit" value="To index">
  </form>
</ul>